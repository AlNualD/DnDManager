<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.DnDManager" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary_bronze</item>
        <item name="colorPrimaryVariant">@color/primary_bronze</item>
        <item name="colorOnPrimary">@color/background_light</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/primary_bronze_70</item>
        <item name="colorSecondaryVariant">@color/primary_bronze_45</item>
        <item name="colorOnSecondary">@color/background_light</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:colorAccent">@color/primary_bronze</item>
        <item name="android:textColorPrimary">@color/primary_bronze</item>
        <item name="android:textColorSecondary">@color/primary_bronze_70</item>
        <item name="hintTextColor">@color/primary_bronze_45</item>
        <item name="hintTextAppearance">@style/FontBodyCaption</item>
        <item name="background">@color/background_light</item>
        <item name="colorSurface">@color/primary_bronze_45</item>
        <item name="colorOnSurface">@color/primary_bronze_70</item>
    </style>
</resources>